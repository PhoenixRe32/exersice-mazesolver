Wanted to make a note that I changed the text files so the lines that define the
dimensions, start point and end point of the maze are always X-axis first and 
Y-axis second. So the first line is WIDTH HEIGHT and the points are X Y.